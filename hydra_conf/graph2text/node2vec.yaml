method: node2vec
walk_length: 40 # Length of walk per source
num_walks: 10
p: 1 # Return hyperparameter
q: 1 # Inout hyperparameter
weighted: False # If false, treat all graph weights as 1
random_seed: ${random_seed}

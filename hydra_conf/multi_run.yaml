defaults:
  - dataset: sample
  - embedding: small
  - normalize: leva
  - table2graph: leva
  - graph2text: node2vec
  - text2model: word2vec
  - _self_
  - pipeline: multi
  - override hydra/sweeper: pipeline

multirun: true

hydra:
  job:
    chdir: true
  mode: MULTIRUN

defaults:
  - dataset: sample
  - normalize: leva
  - table2graph: leva
  - graph2text: node2vec
  - _self_
  - pipeline: multi
  - override hydra/sweeper: pipeline

hydra:
  job:
    chdir: true
  mode: MULTIRUN

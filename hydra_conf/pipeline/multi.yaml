# @package _global_
hydra:
  sweep:
    subdir: ${pipeline_subdir}
  sweeper:
    pipelines:
      - [normalize, table2graph, graph2text, text2model, model2emb, classification]
      - [normalize, table2graph, graph2model, model2emb, classification]
    stage_variants:
      normalize: [leva]
      table2graph: [leva, embdi]
      graph2model: [ProNE]
      graph2text: [node2vec]
      text2model: [word2vec]
      model2emb: [word2vec]
      classification: [basic]

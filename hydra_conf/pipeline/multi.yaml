# @package _global_
hydra:
  sweep:
    subdir: ${pipeline_subdir}
  sweeper:
    pipelines:
      - [normalize, table2graph, graph2text]
    stage_variants:
      normalize: [leva]
      table2graph: [leva]
      graph2text: [node2vec]

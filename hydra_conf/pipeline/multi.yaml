# @package _global_
hydra:
  sweep:
    subdir: ${pipeline_subdir}
  sweeper:
    pipelines:
      - [normalize, table2graph, graph2text, text2model]
    stage_variants:
      normalize: [leva]
      table2graph: [leva]
      graph2text: [node2vec]
      text2model: [word2vec]

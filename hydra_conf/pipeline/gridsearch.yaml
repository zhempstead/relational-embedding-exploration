# @package _global_
hydra:
    sweep:
        subdir: ${pipeline_subdir}
    sweeper:
        pipeline: [normalize, table2graph, graph2text, text2model, model2emb, classification]
    launcher:
        n_jobs: 32

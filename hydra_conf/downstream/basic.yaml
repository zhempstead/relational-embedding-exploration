task: ${dataset.downstream_task}
methods_by_task:
  classification:
    - logistic
    #- random_forest
    #- nn
  regression:
    - elastic
    #- nn
model_suffix: ${model2emb.model_suffix}
random_seed: ${global.random_seed}
cv_splits: 5
